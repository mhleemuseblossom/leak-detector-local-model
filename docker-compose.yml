version: '3.8'

services:
  sentinel:
    build: .
    container_name: sentinel-leak-detector
    ports:
      - "8000:8000"
    environment:
      - GEMINI_API_KEY=${GEMINI_API_KEY}
    volumes:
      - ./data:/app/data
    restart: unless-stopped
